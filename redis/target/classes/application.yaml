spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password: 123456
    # springBoot默认使用lettuce方式的实现，否则需要另外引入redis的依赖
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: 100